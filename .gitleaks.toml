[extend]
useDefault = true

[[allowlists]]
description = "Pinned LTX workflow integrity hashes, not credentials"
regexTarget = "line"
regexes = [
  '''(?i)^\s*(?:"api_sha256"|EXPECTED_API_WORKFLOW_SHA256)\s*(?::|=)\s*"[0-9a-f]{64}"[,]?\s*$''',
]
